{
  "name": "intelliquiz",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend-new && npm install",
    "build": "cd frontend-new && npm install && npm run build",
    "start": "cd backend && npm start",
    "production": "cd backend && node server.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
